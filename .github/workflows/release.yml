name: Auto Tag Release and Create Release with JAR

on:
  push:
    branches:
      - release

jobs:
  bumpTag:
    runs-on: ubuntu-latest
    outputs:
      tag_name: ${{ steps.tag_version.outputs.TAG }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Bump version and push tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

  create_release:
    needs: bumpTag
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
      pull-requests: write
      issues: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Java
        uses: actions/setup-java@v2
        with:
          java-version: '17'  # Set the Java version according to your requirements
          distribution: 'temurin'

      - name: Set up Maven
        run: |
          sudo apt-get install -y maven

      - name: Create Downloads directory
        run: mkdir -p /home/runner/Downloads

      - name: Build project
        run: mvn clean install

      - name: Create Release
        id: create_release
        run: |
          RELEASE_NAME=${{ needs.bumpTag.outputs.tag_name }}
          echo "RELEASE_NAME=$RELEASE_NAME" >> $GITHUB_ENV
          gh release create $RELEASE_NAME target/*.jar -t "$RELEASE_NAME" -n "Release $RELEASE_NAME"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload JAR to Release
        run: gh release upload $RELEASE_NAME target/*.jar
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

